# Backend Modules

This directory follows a domain-driven structure.
Each module encapsulates:
- API routes
- business logic
- validations
- data access
- authorization rules

Modules are added incrementally as features are implemented.